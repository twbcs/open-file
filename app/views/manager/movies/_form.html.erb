<%= form_with(model: [:manager, movie], local: true) do |form| %>
  <% if movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
      <% movie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :dir_name %>
    <%= form.text_field :dir_name, class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :tag_names %>
    <%= form.text_field :tag_names, name: 'movie[tag_names][]', value: movie.tag_names, class: 'form-control' %>
  </div>
  <div class='tag_list'>
    <% Gutentag::Tag.all.each do |t| %>
      <% unless movie.tag_names.include? t.name %>
        <span class="tag active badge badge-primary"><%= t.name %></span>
      <% end %>
    <% end %>
  </div>

  <div class="field">
    <% if movie.image.attached? %>
      <%= link_to remove_manager_movie_path(movie), class: "btn btn-primary" do %>
        <i class="fas fa-trash-alt"></i> 移除檔案
      <% end%>
      <%= image_tag @movie.image.variant(resize: "200x400") %><br>
    <% end %>
    <%= form.file_field :image %>
  </div>
<br>
  <div class="actions">
    <%= form.submit nil, class: "btn btn-primary" %>
  </div>
  <br>
<% end %>
